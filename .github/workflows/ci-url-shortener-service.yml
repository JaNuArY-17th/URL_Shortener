name: CI URL Shortener Service
on:
  push:
    paths:
      - 'src/UrlShortenerService/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '7.0.x'
      - name: Restore dependencies
        run: dotnet restore src/UrlShortenerService/UrlShortenerService.csproj
      - name: Build
        run: dotnet build --no-restore src/UrlShortenerService/UrlShortenerService.csproj
      - name: Test
        run: echo "No tests yet" 